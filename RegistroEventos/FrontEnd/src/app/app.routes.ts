import { Routes } from '@angular/router';
import { RegisterEventoComponent } from './register-evento/register-evento.component';


export const routes: Routes = [
    {
        path: 'registerEvento',
        component: RegisterEventoComponent
    }

];
